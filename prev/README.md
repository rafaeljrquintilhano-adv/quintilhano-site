# Public `/prev/` route

This folder is the production route for the Auxilio-Acidente landing page.

The page reuses the canonical assets and script from
`landing-pages/auxilio-acidente/` to avoid duplicating image files.
